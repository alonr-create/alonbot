---
phase: 02-sales-conversation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/ai/claude-client.ts
  - src/ai/system-prompt.ts
  - src/ai/conversation.ts
  - src/whatsapp/message-batcher.ts
  - src/whatsapp/message-handler.ts
  - src/ai/__tests__/conversation.test.ts
  - src/ai/__tests__/system-prompt.test.ts
  - src/whatsapp/__tests__/message-batcher.test.ts
autonomous: true
requirements: [AI-01, AI-02, AI-03, AI-04, AI-05]

must_haves:
  truths:
    - "When a lead sends a message, bot waits 8 seconds after last message then responds to the full batch"
    - "Bot response is generated by Claude using last 20 messages as context"
    - "System prompt contains all Alon.dev services with exact price ranges from CONTEXT.md"
    - "Bot introduces itself referencing the lead's stated interest on first contact"
    - "Conversation history persists in SQLite and survives bot restart"
    - "Monday.com status updates as conversation progresses (contacted, in-conversation, quote-sent)"
  artifacts:
    - path: "src/ai/claude-client.ts"
      provides: "Anthropic SDK wrapper"
      exports: ["generateResponse"]
    - path: "src/ai/system-prompt.ts"
      provides: "Full Alon.dev service catalog system prompt with price ranges"
      exports: ["buildSystemPrompt"]
    - path: "src/ai/conversation.ts"
      provides: "Conversation orchestrator — history from DB, Claude call, response storage, status sync"
      exports: ["handleConversation"]
    - path: "src/whatsapp/message-batcher.ts"
      provides: "Per-phone 8-second debounce timer"
      exports: ["addMessageToBatch"]
    - path: "src/whatsapp/message-handler.ts"
      provides: "Rewritten message handler routing to batcher + AI instead of TEST_RESPONSE"
      exports: ["setupMessageHandler"]
  key_links:
    - from: "src/whatsapp/message-handler.ts"
      to: "src/whatsapp/message-batcher.ts"
      via: "addMessageToBatch on every incoming message"
      pattern: "addMessageToBatch"
    - from: "src/whatsapp/message-batcher.ts"
      to: "src/ai/conversation.ts"
      via: "onBatchReady callback triggers handleConversation"
      pattern: "handleConversation"
    - from: "src/ai/conversation.ts"
      to: "src/ai/claude-client.ts"
      via: "generateResponse with history + system prompt"
      pattern: "generateResponse"
    - from: "src/ai/conversation.ts"
      to: "src/monday/api.ts"
      via: "updateMondayStatus when conversation progresses"
      pattern: "updateMondayStatus"
    - from: "src/ai/conversation.ts"
      to: "src/whatsapp/rate-limiter.ts"
      via: "sendWithTyping to deliver Claude's response"
      pattern: "sendWithTyping"

user_setup:
  - service: anthropic
    why: "Claude API for AI sales conversations"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API Keys -> Create key"
  - service: monday
    why: "Monday.com API for lead data and status updates"
    env_vars:
      - name: MONDAY_API_TOKEN
        source: "Monday.com -> Profile -> Developers -> My Access Tokens"
      - name: MONDAY_BOARD_ID
        source: "Monday.com -> Open leads board -> Board URL contains board ID"
      - name: MONDAY_STATUS_COLUMN_ID
        source: "Monday.com -> Board settings -> Columns -> Status column ID (default: 'status')"
---

<objective>
AI-powered Hebrew sales conversation engine with message batching and full wiring.

Purpose: Transform the bot from a static test responder into an autonomous AI salesperson. Incoming WhatsApp messages are batched (8-second debounce), sent to Claude with conversation history and Alon.dev service catalog, and the response is delivered back with typing simulation. Monday.com status updates flow automatically as conversations progress.

Output: Claude client, system prompt, conversation orchestrator, message batcher, rewritten message handler.
</objective>

<execution_context>
@/Users/oakhome/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oakhome/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sales-conversation/02-CONTEXT.md
@.planning/phases/02-sales-conversation/02-RESEARCH.md
@.planning/phases/02-sales-conversation/02-VALIDATION.md
@.planning/phases/02-sales-conversation/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 02-01 (must complete first) -->

From src/monday/types.ts (created by Plan 01):
```typescript
export type LeadStatus = 'new' | 'contacted' | 'in-conversation' | 'quote-sent' | 'meeting-scheduled' | 'escalated';
export interface MondayWebhookPayload { challenge?: string; event?: { type: string; pulseId: number; boardId: number; pulseName: string } }
export interface MondayItem { name: string; phone: string; interest: string }
```

From src/monday/api.ts (created by Plan 01):
```typescript
export async function fetchMondayItem(itemId: number): Promise<MondayItem>;
export async function updateMondayStatus(itemId: number, boardId: number, status: LeadStatus): Promise<void>;
```

From src/monday/webhook-handler.ts (created by Plan 01):
```typescript
export const mondayWebhookRouter: Router;
export function setOnNewLeadCallback(fn: (lead: { phone: string; name: string; interest: string }) => Promise<void>): void;
```

<!-- Existing code from Phase 1 -->

From src/whatsapp/rate-limiter.ts:
```typescript
export async function sendWithTyping(sock: WASocket, jid: string, text: string): Promise<void>;
```

From src/whatsapp/message-handler.ts (will be REWRITTEN):
```typescript
export function setupMessageHandler(sock: WASocket): void;
// Currently sends TEST_RESPONSE -- will be replaced with batcher + AI flow
```

From src/db/index.ts:
```typescript
export function getDb(): Database.Database;
```

From src/config.ts (extended by Plan 01):
```typescript
export const config = {
  // ... existing fields ...
  anthropicApiKey: string;
  mondayApiToken: string;
  mondayBoardId: string;
  mondayStatusColumnId: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Claude client, system prompt, and message batcher</name>
  <files>src/ai/claude-client.ts, src/ai/system-prompt.ts, src/whatsapp/message-batcher.ts, src/ai/__tests__/system-prompt.test.ts, src/whatsapp/__tests__/message-batcher.test.ts</files>
  <behavior>
    - System prompt contains all Alon.dev services with exact prices from CONTEXT.md
    - System prompt has {lead_name} and {lead_interest} placeholders filled by buildSystemPrompt(name, interest)
    - System prompt enforces price guardrails (never below min, never above max)
    - System prompt sets personality: aggressive sales, Hebrew-first, emojis, urgency
    - generateResponse(messages, systemPrompt) calls Claude claude-sonnet-4-20250514 and returns text
    - addMessageToBatch(phone, text, onBatchReady) debounces at 8 seconds per phone
    - Multiple messages within 8 seconds are combined into one batch
    - Batch callback receives phone + array of message texts
    - Timer resets on each new message within window
  </behavior>
  <action>
    1. Create src/ai/system-prompt.ts:
       - Export `buildSystemPrompt(leadName: string, leadInterest: string): string`
       - Full Hebrew system prompt with Alon.dev service catalog (exact prices from CONTEXT.md):
         - Landing pages: 2,000-5,000, Business sites: 5,000-15,000, E-commerce: 10,000-30,000
         - Mobile apps: 15,000-50,000, Web apps: 10,000-40,000
         - Browser games: 5,000-20,000, Mobile games: 20,000-60,000
         - Automation: 3,000-10,000, CRM: 5,000-15,000
         - Social media: 2,000-5,000/month, SEO: 3,000-8,000/month
       - Price guardrail rules: never below min, never above max, use ranges when scope unclear
       - Personality: aggressive sales, pushy not rude, urgency, emojis, Hebrew-first informal
       - Presents Alon as solo entrepreneur + AI = power of full team
       - Includes lead name and interest in context section
       - Handles media messages: acknowledge, explain text-only for now
       - Non-Hebrew: respond in detected language (Claude's discretion per CONTEXT.md)

    2. Create src/ai/claude-client.ts:
       - `import Anthropic from '@anthropic-ai/sdk'`
       - Lazy singleton: create client on first call using config.anthropicApiKey
       - Export `generateResponse(messages: Array<{role: 'user'|'assistant', content: string}>, systemPrompt: string): Promise<string>`
       - Calls client.messages.create with model 'claude-sonnet-4-20250514', max_tokens 1024, temperature 0.7
       - Extracts text from response.content[0].text
       - Logs token usage with pino
       - On error: log and return a Hebrew fallback message ("מצטער, נתקלתי בבעיה טכנית. אלון יחזור אליך בקרוב!")

    3. Create src/whatsapp/message-batcher.ts:
       - `const pendingBatches = new Map<string, { timer: NodeJS.Timeout; messages: string[] }>()`
       - Export `addMessageToBatch(phone: string, text: string, onBatchReady: (phone: string, messages: string[]) => void): void`
       - If phone already in map: clearTimeout, push message to array
       - If phone not in map: create new entry with [text]
       - Set new setTimeout for 8000ms that deletes entry and calls onBatchReady(phone, messages)
       - Export `getPendingCount(): number` for monitoring
       - Export `clearAllBatches(): void` for testing cleanup

    4. Create src/ai/__tests__/system-prompt.test.ts:
       - Test buildSystemPrompt includes all service categories
       - Test all price ranges present (2000, 5000, 15000, etc.)
       - Test lead name and interest are interpolated
       - Test price guardrail instructions present ("לעולם אל תציע מחיר מתחת למינימום")

    5. Create src/whatsapp/__tests__/message-batcher.test.ts:
       - Use vi.useFakeTimers()
       - Test single message triggers callback after 8 seconds
       - Test multiple messages within 8s are batched together
       - Test timer resets on each new message
       - Test different phones have independent batches
       - Test clearAllBatches stops all timers

    6. Install @anthropic-ai/sdk: `npm install @anthropic-ai/sdk`
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>System prompt has all Alon.dev services+prices+personality, Claude client wraps SDK with error handling, batcher debounces at 8s per phone, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Conversation orchestrator and message handler rewrite</name>
  <files>src/ai/conversation.ts, src/whatsapp/message-handler.ts, src/ai/__tests__/conversation.test.ts</files>
  <behavior>
    - handleConversation(phone, batchedMessages, sock) builds history from DB, calls Claude, sends response, stores all messages, updates Monday status
    - Last 20 messages from DB used as conversation history for Claude context
    - Batched messages joined with newline as single user message
    - Both incoming batch and outgoing response stored in messages table
    - Monday.com status auto-progression: new->contacted (first outbound), contacted->in-conversation (lead replies), in-conversation->quote-sent (price mentioned)
    - Quote detection: response contains shekel sign or price pattern
    - Message handler rewrite: routes to batcher instead of TEST_RESPONSE
    - Message handler sets up onNewLead callback for Monday webhook auto-intro
    - First message to Monday lead references their interest
  </behavior>
  <action>
    1. Create src/ai/conversation.ts:
       - Export `handleConversation(phone: string, batchedMessages: string[], sock: WASocket): Promise<void>`
       - Get DB via getDb()
       - Look up lead by phone: `SELECT * FROM leads WHERE phone = ?`
       - Build system prompt using buildSystemPrompt(lead.name || 'לקוח', lead.interest || '')
       - Fetch last 20 messages: `SELECT direction, content FROM messages WHERE phone = ? ORDER BY created_at DESC LIMIT 20` then .reverse()
       - Map to Claude format: direction 'in' = role 'user', direction 'out' = role 'assistant'
       - Add batched messages joined with '\n' as final user message
       - Call generateResponse(messages, systemPrompt)
       - Send response via sendWithTyping(sock, phone + '@s.whatsapp.net', response)
       - Store all batched incoming messages in DB (each as separate INSERT for accurate history)
       - Store outgoing response in DB
       - Status progression logic:
         - If lead.status === 'new': update to 'contacted', call updateMondayStatus if monday_item_id exists
         - If lead.status === 'contacted': update to 'in-conversation', call updateMondayStatus
         - If response contains /₪[\d,]+/ or price patterns and lead.status !== 'quote-sent': update to 'quote-sent', call updateMondayStatus
       - Update lead.updated_at on every interaction
       - All Monday status updates are fire-and-forget (non-blocking, errors logged)

       - Export `sendFirstMessage(phone: string, name: string, interest: string, sock: WASocket): Promise<void>`
       - For Monday.com webhook-triggered first contact
       - Build a personalized intro referencing their interest (e.g., "היי {name}! ראיתי שאתה מעוניין ב{interest} - מעולה!")
       - Uses Claude to generate the intro message with system prompt context
       - Sends via sendWithTyping, stores in DB
       - Updates status to 'contacted', syncs to Monday.com

    2. Rewrite src/whatsapp/message-handler.ts:
       - Keep the same structure: sock.ev.on('messages.upsert', ...)
       - Keep: skip fromMe, skip !msg.message, extract text and phone
       - Remove: TEST_RESPONSE constant, direct sendWithTyping call
       - Add: import addMessageToBatch from message-batcher
       - Add: import handleConversation from conversation
       - On incoming text: store message in DB immediately (for restart resilience per Pitfall 5)
       - Then call addMessageToBatch(phone, text, async (batchPhone, messages) => { await handleConversation(batchPhone, messages, sock); })
       - Create/update lead record same as before (check existing, create if new)
       - Add: import setOnNewLeadCallback from webhook-handler
       - In setupMessageHandler: call setOnNewLeadCallback with async function that calls sendFirstMessage
       - Handle non-text messages: if msg.message has imageMessage/audioMessage/videoMessage/documentMessage, send a text acknowledging: "קיבלתי! כרגע אני עובד רק עם הודעות טקסט, אבל אשמח לעזור - ספר לי במילים מה אתה מחפש"

    3. Create src/ai/__tests__/conversation.test.ts:
       - Mock @anthropic-ai/sdk (vi.mock) to return controlled responses
       - Mock sendWithTyping (vi.mock ../whatsapp/rate-limiter)
       - Use in-memory SQLite (same pattern as Phase 1 tests)
       - Test handleConversation: builds correct message history from DB
       - Test handleConversation: stores both incoming and outgoing messages
       - Test status progression: new -> contacted -> in-conversation
       - Test quote detection: response with ₪ triggers quote-sent status
       - Test sendFirstMessage: sends personalized intro with lead interest
       - Test last-20-message limit is respected
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>Message handler routes to batcher+AI, conversation orchestrator manages full flow (history->Claude->send->store->status), Monday status auto-progresses, first message references lead interest, all tests pass including Phase 1 tests</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` — all tests pass (Phase 1 + Phase 2)
- `npx tsc --noEmit` — TypeScript compiles clean
- Message flow: incoming text -> batcher (8s debounce) -> conversation (DB history + Claude) -> sendWithTyping -> store in DB
- Monday.com flow: webhook -> create lead -> auto-intro via sendFirstMessage -> status sync
</verification>

<success_criteria>
- Bot responds to WhatsApp messages with Claude-generated Hebrew sales conversation (not TEST_RESPONSE)
- Multi-message batching: rapid messages collected for 8 seconds then processed as one
- Conversation history from DB sent to Claude (last 20 messages)
- System prompt has complete Alon.dev service catalog with exact price ranges
- Monday.com lead gets personalized first message referencing their interest
- Lead status progresses automatically: new -> contacted -> in-conversation -> quote-sent
- Monday.com status column updated via GraphQL
- All messages persisted in SQLite
- All existing Phase 1 tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-sales-conversation/02-02-SUMMARY.md`
</output>
