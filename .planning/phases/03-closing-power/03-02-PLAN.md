---
phase: 03-closing-power
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/ai/conversation.ts
  - src/ai/system-prompt.ts
autonomous: true
requirements:
  - CAL-01
  - CAL-02
  - CAL-03
  - ESC-01
  - ESC-02
  - ESC-03

must_haves:
  truths:
    - "When Claude response contains [BOOK:YYYY-MM-DD:HH:mm], conversation orchestrator calls bookMeeting and confirms to lead"
    - "When Claude response contains [ESCALATE], conversation orchestrator triggers escalation flow"
    - "System prompt includes current Israel time and business hours context"
    - "System prompt includes available calendar slots when during business hours"
    - "System prompt instructs Claude to use [BOOK:...] and [ESCALATE] markers"
    - "Escalation count increments when lead sends a non-substantive message and resets on substantive engagement"
    - "Monday.com status updates to meeting-scheduled after successful booking"
  artifacts:
    - path: "src/ai/conversation.ts"
      provides: "Extended conversation handler with calendar booking and escalation"
      exports: ["handleConversation", "sendFirstMessage"]
    - path: "src/ai/system-prompt.ts"
      provides: "Extended system prompt with calendar, business hours, and escalation instructions"
      exports: ["buildSystemPrompt"]
  key_links:
    - from: "src/ai/conversation.ts"
      to: "src/calendar/api.ts"
      via: "bookMeeting call on [BOOK:...] marker detection"
      pattern: "\\[BOOK:"
    - from: "src/ai/conversation.ts"
      to: "src/escalation/handler.ts"
      via: "triggerEscalation call on [ESCALATE] marker or count threshold"
      pattern: "\\[ESCALATE\\]|triggerEscalation"
    - from: "src/ai/system-prompt.ts"
      to: "src/calendar/business-hours.ts"
      via: "isBusinessHours and formatIsraelTime calls"
      pattern: "isBusinessHours|formatIsraelTime"
    - from: "src/ai/system-prompt.ts"
      to: "src/calendar/api.ts"
      via: "getAvailableSlots for slot injection"
      pattern: "getAvailableSlots"
---

<objective>
Wire calendar booking and escalation into the conversation flow.

Purpose: Integrate the standalone calendar and escalation modules (from Plan 01) into the conversation orchestrator and system prompt so Claude naturally offers meeting times and the bot detects escalation triggers.

Output: Extended `conversation.ts` with marker detection and action execution. Extended `system-prompt.ts` with calendar slots, business hours context, and marker instructions.
</objective>

<execution_context>
@/Users/oakhome/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oakhome/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-closing-power/03-RESEARCH.md
@.planning/phases/03-closing-power/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (new modules created) -->

From src/calendar/api.ts:
```typescript
interface TimeSlot { date: string; time: string; dayName: string; }
interface BookingResult { success: boolean; eventId?: string; error?: string; }
export async function getAvailableSlots(days?: number): Promise<TimeSlot[]>;
export async function bookMeeting(date: string, time: string, leadName: string, phone: string, interest: string, summary: string): Promise<BookingResult>;
```

From src/calendar/business-hours.ts:
```typescript
export function isBusinessHours(date?: Date): boolean;
export function getNextBusinessDay(from?: Date): Date;
export function formatIsraelTime(date?: Date): string;
```

From src/escalation/handler.ts:
```typescript
export function incrementEscalationCount(phone: string): number;
export function resetEscalationCount(phone: string): void;
export function shouldEscalate(phone: string, messageText: string): { escalate: boolean; reason: 'count' | 'human-request' | null };
export async function triggerEscalation(phone: string, leadName: string, messages: Array<{role: 'user' | 'assistant', content: string}>, sock: WASocket, mondayItemId?: number, mondayBoardId?: number): Promise<void>;
```

From src/escalation/summary.ts:
```typescript
export async function generateEscalationSummary(messages: Array<{role: 'user' | 'assistant', content: string}>, leadName: string): Promise<string>;
```

<!-- Existing interfaces being modified -->

From src/ai/system-prompt.ts (current):
```typescript
export function buildSystemPrompt(leadName: string, leadInterest: string): string;
```

From src/ai/conversation.ts (current):
```typescript
export async function handleConversation(phone: string, batchedMessages: string[], sock: WASocket): Promise<void>;
export async function sendFirstMessage(phone: string, name: string, interest: string, sock: WASocket): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend system prompt with calendar, business hours, and escalation markers</name>
  <files>src/ai/system-prompt.ts</files>
  <action>
Modify `buildSystemPrompt` to become `async` (needs to fetch available slots). New signature:
```typescript
export async function buildSystemPrompt(leadName: string, leadInterest: string): Promise<string>;
```

Add to the end of the system prompt template:

1. **Business hours context section:**
   ```
   ## שעות פעילות
   השעה עכשיו: ${formatIsraelTime()}
   ${isBusinessHours() ? 'אנחנו בשעות פעילות — אפשר להציע פגישות ולדחוף לסגירה.' : 'אנחנו מחוץ לשעות פעילות — תגיב בחום אבל אל תדחוף לפגישה או שיחת טלפון. אמור שתחזור עם הצעה מחר בשעות הפעילות.'}
   ```

2. **Available slots section (only during business hours and when slots available):**
   Call `getAvailableSlots(3)` and if slots exist, add:
   ```
   ## זמנים פנויים לפגישת היכרות עם אלון
   הזמנים הבאים פנויים: ${formatted slots with Hebrew day names}
   כשהלקוח מאשר זמן, הוסף בסוף ההודעה שלך בדיוק כך: [BOOK:YYYY-MM-DD:HH:mm]
   לדוגמה: [BOOK:2026-03-10:10:00]
   ```
   If no slots or not business hours, omit this section.

3. **Escalation markers section:**
   ```
   ## הסלמה
   אם הלקוח מבקש לדבר עם אדם אמיתי, או שאתה מרגיש שהשיחה לא מתקדמת ואין סיכוי לסגור, הוסף בסוף ההודעה שלך: [ESCALATE]
   חשוב: [ESCALATE] ו-[BOOK:...] תמיד בסוף ההודעה, אחרי הטקסט ללקוח.
   ```

4. **Meeting type:** Default to phone call. Bot says "אלון יתקשר אליך" (per research recommendation — no Zoom API needed).

Import `isBusinessHours`, `formatIsraelTime` from `../calendar/business-hours.js` and `getAvailableSlots` from `../calendar/api.js`.

Note: Making buildSystemPrompt async requires updating all callers (done in Task 2).
  </action>
  <verify>
    <automated>cd /Users/oakhome/קלוד\ עבודות/alon-dev-whatsapp && npx tsc --noEmit</automated>
  </verify>
  <done>System prompt includes current Israel time, business hours context, available calendar slots (when applicable), and [BOOK:...]/[ESCALATE] marker instructions. buildSystemPrompt is async.</done>
</task>

<task type="auto">
  <name>Task 2: Wire conversation orchestrator with marker detection, booking, and escalation</name>
  <files>src/ai/conversation.ts</files>
  <action>
Modify `handleConversation` to:

1. **Await system prompt** (now async):
   ```typescript
   const systemPrompt = await buildSystemPrompt(leadName, leadInterest);
   ```

2. **Check escalation BEFORE calling Claude:**
   After receiving batched messages, check `shouldEscalate(phone, batchedText)`:
   - If `escalate` is true with reason `'human-request'`: trigger escalation immediately, return early.
   - If `escalate` is true with reason `'count'`: trigger escalation, return early.
   - Otherwise continue normal flow.

3. **Escalation count management:**
   - After a successful conversation turn (Claude responded, lead is engaged), call `resetEscalationCount(phone)` — the lead is actively engaging.
   - The escalation count is incremented elsewhere — specifically, a "failed attempt" means the bot sent a message but the lead didn't respond meaningfully. For this MVP, implement a simple heuristic: if the incoming message is very short (< 5 chars) AND the lead is already in `in-conversation` status, call `incrementEscalationCount(phone)`. If the message is substantive (>= 5 chars), call `resetEscalationCount(phone)`.

4. **Parse [BOOK:...] marker from Claude response:**
   ```typescript
   const bookMatch = response.match(/\[BOOK:(\d{4}-\d{2}-\d{2}):(\d{2}:\d{2})\]/);
   if (bookMatch) {
     const [, date, time] = bookMatch;
     const cleanResponse = response.replace(/\[BOOK:[^\]]+\]/, '').trim();
     // Send the clean response first
     await sendWithTyping(sock, jid, cleanResponse);
     // Book the meeting
     const result = await bookMeeting(date, time, leadName, phone, leadInterest, 'Discovery call');
     if (result.success) {
       await sendWithTyping(sock, jid, `מעולה! הפגישה נקבעה ל-${date} בשעה ${time}. אלון יתקשר אליך 📞`);
       // Update status
       newStatus = 'meeting-scheduled';
     } else {
       await sendWithTyping(sock, jid, 'סליחה, הייתה בעיה עם קביעת הפגישה. אלון יחזור אליך עם זמנים מעודכנים.');
     }
     // Store messages and return (skip normal send path)
   }
   ```

5. **Parse [ESCALATE] marker from Claude response:**
   ```typescript
   if (response.includes('[ESCALATE]')) {
     const cleanResponse = response.replace('[ESCALATE]', '').trim();
     await sendWithTyping(sock, jid, cleanResponse);
     await triggerEscalation(phone, leadName, messages, sock, lead?.monday_item_id, lead?.monday_board_id);
     // Store messages, update status, return
   }
   ```

6. **Status progression update:** Add `meeting-scheduled` and `escalated` to the status progression logic (already handled by the booking/escalation paths above, but ensure no conflict with existing quote detection).

7. **Update sendFirstMessage** to use `await buildSystemPrompt(name, interest)`.

Import `bookMeeting` from `../calendar/api.js`, `shouldEscalate`, `triggerEscalation`, `resetEscalationCount`, `incrementEscalationCount` from `../escalation/handler.js`.

IMPORTANT: Keep the existing fire-and-forget pattern for Monday.com updates. Keep the existing message storage pattern. Keep the existing rate limiter usage. Do not break any existing behavior — only extend.
  </action>
  <verify>
    <automated>cd /Users/oakhome/קלוד\ עבודות/alon-dev-whatsapp && npx vitest run --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>handleConversation detects [BOOK:...] and [ESCALATE] markers, executes booking/escalation flows, manages escalation counts. sendFirstMessage uses async system prompt. All existing tests pass. TypeScript compiles.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/oakhome/קלוד\ עבודות/alon-dev-whatsapp && npx vitest run --reporter=verbose && npx tsc --noEmit
```
Full test suite passes. TypeScript compiles. Calendar and escalation modules correctly wired into conversation flow.
</verification>

<success_criteria>
- Claude response with [BOOK:2026-03-10:10:00] triggers bookMeeting and sends confirmation
- Claude response with [ESCALATE] triggers escalation flow (summary + Telegram + Monday.com)
- System prompt includes current Israel time and available slots during business hours
- System prompt omits meeting suggestions outside business hours
- Escalation count tracks engagement, resets on substantive messages
- All existing Phase 1+2 tests still pass
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-closing-power/03-02-SUMMARY.md`
</output>
