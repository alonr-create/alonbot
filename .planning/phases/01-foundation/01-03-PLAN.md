---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/http/server.ts
  - src/http/routes/health.ts
  - src/http/routes/qr.ts
  - src/index.ts
  - Dockerfile
  - .dockerignore
  - src/http/__tests__/health.test.ts
autonomous: false
requirements:
  - INF-02
  - INF-03

must_haves:
  truths:
    - "Health endpoint returns JSON with WhatsApp status, DB health, uptime, and memory usage"
    - "QR web page at /qr shows QR code when connecting and 'connected' status when linked"
    - "Docker image builds and runs successfully"
    - "Bot starts up, connects all subsystems, and is ready to receive messages"
  artifacts:
    - path: "src/http/server.ts"
      provides: "Express v5 app with mounted routes"
      exports: ["createServer"]
    - path: "src/http/routes/health.ts"
      provides: "GET /health JSON response"
      exports: ["healthRouter"]
    - path: "src/http/routes/qr.ts"
      provides: "GET /qr web page + GET /api/qr-status JSON"
      exports: ["qrRouter"]
    - path: "src/index.ts"
      provides: "Entry point wiring all subsystems together"
    - path: "Dockerfile"
      provides: "Docker build for Railway deployment"
  key_links:
    - from: "src/http/routes/health.ts"
      to: "src/whatsapp/qr.ts"
      via: "getConnectionStatus for WhatsApp health"
      pattern: "getConnectionStatus"
    - from: "src/http/routes/health.ts"
      to: "src/db/index.ts"
      via: "checkDbHealth for database health"
      pattern: "checkDbHealth"
    - from: "src/http/routes/qr.ts"
      to: "src/whatsapp/qr.ts"
      via: "getCurrentQR and getConnectionStatus for QR page"
      pattern: "getCurrentQR|getConnectionStatus"
    - from: "src/index.ts"
      to: "src/whatsapp/connection.ts"
      via: "connectWhatsApp called on startup"
      pattern: "connectWhatsApp"
    - from: "src/index.ts"
      to: "src/http/server.ts"
      via: "createServer called on startup"
      pattern: "createServer"
---

<objective>
Create the HTTP server (Express v5) with health endpoint and QR web page, the main entry point that wires everything together, and the Docker deployment configuration for Railway.

Purpose: This completes Phase 1 by making the bot deployable and monitorable. The health endpoint enables Railway health checks, the QR page enables remote session setup, and the entry point ties all subsystems together.
Output: Fully runnable bot with HTTP endpoints and Docker image ready for Railway.
</objective>

<execution_context>
@/Users/oakhome/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oakhome/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Express server with health endpoint, QR web page, and entry point</name>
  <files>src/http/server.ts, src/http/routes/health.ts, src/http/routes/qr.ts, src/index.ts, src/http/__tests__/health.test.ts</files>
  <behavior>
    - Health test: GET /health returns JSON with status, whatsapp, database, uptime, memory, timestamp fields
    - Health test: status is 'ok' when both whatsapp and db healthy, 'degraded' otherwise
    - Health test: returns 200 status code always (Railway needs 200 for health check)
  </behavior>
  <action>
    1. Create src/http/routes/health.ts:
       - Import getConnectionStatus from whatsapp/qr.ts
       - Import checkDbHealth, db from db/index.ts
       - Export healthRouter (Express Router)
       - GET /health returns JSON:
         ```
         {
           status: waConnected && dbHealthy ? 'ok' : 'degraded',
           whatsapp: { connected: boolean, status: string },
           database: { healthy: boolean },
           uptime: process.uptime(),
           memory: process.memoryUsage(),
           activeLeads: db.prepare('SELECT COUNT(*) as count FROM leads WHERE status NOT IN ("closed-won","closed-lost")').get(),
           timestamp: new Date().toISOString()
         }
         ```
       - Per user decision: include active leads count and memory usage

    2. Create src/http/routes/qr.ts:
       - Import getCurrentQR, getConnectionStatus from whatsapp/qr.ts
       - Export qrRouter (Express Router)
       - GET /qr: Serve inline HTML page (RTL Hebrew) with:
         - Dark background (#1a1a2e), centered layout
         - Title "AlonBot - WhatsApp"
         - Status indicator (green "connected" / yellow "waiting for QR")
         - QR code image (displayed only when QR available)
         - Polling script: fetch /api/qr-status every 2 seconds, update status and QR image
       - GET /api/qr-status: Return JSON { connected: boolean, qr: string|null, status: string }

    3. Create src/http/server.ts:
       - Import express (v5)
       - Import healthRouter, qrRouter
       - Export function createServer(port: number): http.Server
         - Create express app
         - Mount healthRouter at /
         - Mount qrRouter at /
         - app.listen(port)
         - Log server start
         - Return server

    4. Create src/index.ts (entry point):
       - Import dotenv/config (side-effect import, must be first)
       - Import config
       - Import createLogger
       - Import initDb
       - Import connectWhatsApp
       - Import createServer
       - Main async function:
         a. Log startup with version/environment
         b. Initialize database (initDb)
         c. Start HTTP server (createServer with config.port)
         d. Connect WhatsApp (connectWhatsApp) -- this triggers QR display
         e. Log "Bot ready" with port
         f. Handle SIGINT/SIGTERM: graceful shutdown (close socket, close db, stop server)
       - Call main(), catch errors and log them

    5. Create src/http/__tests__/health.test.ts:
       - Mock whatsapp/qr.ts (getConnectionStatus) and db/index.ts (checkDbHealth, db)
       - Test GET /health returns expected JSON shape
       - Test status 'ok' vs 'degraded' logic
  </action>
  <verify>
    <automated>cd "/Users/oakhome/קלוד עבודות/alon-dev-whatsapp" && npx vitest run src/http/__tests__/health.test.ts --reporter=verbose</automated>
  </verify>
  <done>Express server runs with /health and /qr endpoints. Health returns full status JSON. QR page shows QR code with auto-polling. Entry point wires all subsystems. Health test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create Dockerfile, .dockerignore, and verify full build</name>
  <files>Dockerfile, .dockerignore</files>
  <action>
    1. Create .dockerignore:
       ```
       node_modules
       dist
       data
       .env
       .git
       .planning
       *.md
       ```

    2. Create Dockerfile (adapted from AlonBot pattern):
       ```dockerfile
       FROM node:22-slim

       WORKDIR /app

       # Install build dependencies for better-sqlite3 native module
       RUN apt-get update && apt-get install -y python3 make g++ && rm -rf /var/lib/apt/lists/*

       COPY package.json package-lock.json ./
       RUN npm ci --production=false

       COPY tsconfig.json ./
       COPY src/ src/

       RUN npx tsc
       RUN npm prune --production

       # Create data directory (will be overridden by Railway volume mount)
       RUN mkdir -p /data

       EXPOSE 3000

       CMD ["node", "dist/index.js"]
       ```

    3. Verify TypeScript compiles: `npx tsc --noEmit`
    4. Verify Docker builds: `docker build -t alon-dev-whatsapp-bot .`
    5. Verify Docker runs (quick check): `docker run --rm alon-dev-whatsapp-bot node -e "console.log('ok')"`
  </action>
  <verify>
    <automated>cd "/Users/oakhome/קלוד עבודות/alon-dev-whatsapp" && npx tsc --noEmit && docker build -t alon-dev-whatsapp-bot . 2>&1 | tail -5</automated>
  </verify>
  <done>Dockerfile builds successfully. TypeScript compiles without errors. Docker image runs Node.js. Ready for Railway deployment.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 1 bot end-to-end</name>
  <files>N/A</files>
  <action>Human verifies the complete bot works end-to-end: WhatsApp connection, QR display, health endpoint, message handling, and Docker build.</action>
  <what-built>Complete Phase 1 bot: WhatsApp connection via Baileys with QR display (terminal + web), session persistence, auto-reconnect, rate-limited messaging with typing simulation, SQLite database, health endpoint, structured logging, Docker deployment ready.</what-built>
  <how-to-verify>
    1. Start the bot locally: `npm run dev`
    2. Check terminal for QR code display
    3. Visit http://localhost:3000/qr -- should show QR code with "waiting" status
    4. Visit http://localhost:3000/health -- should show JSON with whatsapp.connected=false, database.healthy=true
    5. (Optional) Scan QR with WhatsApp on a test phone
    6. After scanning: /health should show whatsapp.connected=true, /qr should show "connected"
    7. Send a test message to the bot number -- should receive test response with typing delay
    8. Run `npx vitest run` -- all tests should pass
  </how-to-verify>
  <verify>npx vitest run --reporter=verbose</verify>
  <done>Bot starts, shows QR, health endpoint works, messages received and responded to with typing delay. All tests pass.</done>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
Full test suite: `npx vitest run --reporter=verbose`
TypeScript check: `npx tsc --noEmit`
Docker build: `docker build -t alon-dev-whatsapp-bot .`
Health endpoint: `curl http://localhost:3000/health`
QR page: open http://localhost:3000/qr in browser
</verification>

<success_criteria>
- GET /health returns JSON with all required fields (whatsapp status, db health, uptime, memory, active leads, timestamp)
- GET /qr shows web page with QR code when not connected, "connected" when linked
- GET /api/qr-status returns JSON for polling
- Entry point starts DB, HTTP server, and WhatsApp connection in correct order
- Graceful shutdown on SIGINT/SIGTERM
- Dockerfile builds and image runs
- All tests pass
- Bot is ready for Railway deployment (Phase 1 complete)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
